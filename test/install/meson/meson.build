project('meson_project', 'cpp', version: '1.0.0', default_options: ['cpp_std=c++17'])

# Define the dependency for libralf
libralf_dep = dependency('libralf', required: true)

# Subdirectory for source files
subdir('src')